# Default to Streamlit's light theme.  Tab 4 paints itself dark via CSS
# so the workshop zone reads as a different room of the factory.
[theme]
base = "light"
primaryColor = "#f59e0b"

[server]
headless = false
